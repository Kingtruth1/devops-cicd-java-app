pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/<kingtruth1>/devops-cicd-java-app.git'
            }
        }

        stage('Build') {
            steps {
                dir('app/demo-app') {
                    
        }
    }
}